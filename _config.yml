target: _site

collections:
  - name: posts
    folder: _posts
  - name: books
    folder: _books

data:
  - _data

datasets:
  - name: calendar
    folder: _datasets/calendar
  - name: notes
    folder: _datasets/notes

pageviews:
  - _pages

theme: bootstrap
debug: true

title: Καναβούρας Κωνσταντίνος
